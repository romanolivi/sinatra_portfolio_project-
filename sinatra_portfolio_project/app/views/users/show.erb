<!DOCTYPE html>
<html>
    <head>
    <title>Profile</title>
    <meta charset="UTF-8"> 
    </head>

    <body>
    <% if flash.has?(:message) %>
    <%= flash[:message] %>
    <% end %>

        <h1>Welcome, <%= current_user.username %>!</h1>
        <h4>Here is a List of Your Favorite Theme Parks!</h4>
        <p>Click the park links to learn more</p>

        <h4>Parks: </h4> 
        <ul>
            <% @user.parks.each do |park| %> 
                <li>Name: <a href="/parks/<%= park.id %>><%= park.name%></a></li>
            <% end %> 
        </ul>

        
        
        <p><a href="/logout">Click here</a> to log out</p>
    </body>
</html>